<h3>Comments (<%= @article.comments.size %>)</h3>

<p>
<% @article.comments.each do |c| %>
<%= c.body %><br /><br />
-<%= c.author_name %>
<h5><em>Posted <%= distance_of_time_in_words(c.article.created_at, c.created_at) %> later</h5></em>
<p><%= link_to "delete", article_comment_path(c.article, c), method: :delete %></p>
<% end %>
</p>
